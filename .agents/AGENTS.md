# Custom Agent Rules

- **Commit Messages**: All commit messages for development, debugging, and iterative changes must append ` [skip netlify]` at the end. Only remove this marker when the user explicitly informs you "This is an official online release" (e.g. `feat: my change [skip netlify]`).
